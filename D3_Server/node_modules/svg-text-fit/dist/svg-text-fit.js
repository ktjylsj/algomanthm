// Version 1.1.5 svg-text-fit - https://github.com/vasturiano/svg-text-fit#readme
(function (global, factory) {
	typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports) :
	typeof define === 'function' && define.amd ? define(['exports'], factory) :
	(factory((global.TextFit = {})));
}(this, (function (exports) { 'use strict';

var commonjsGlobal = typeof window !== 'undefined' ? window : typeof global !== 'undefined' ? global : typeof self !== 'undefined' ? self : {};



function unwrapExports (x) {
	return x && x.__esModule && Object.prototype.hasOwnProperty.call(x, 'default') ? x['default'] : x;
}

function createCommonjsModule(fn, module) {
	return module = { exports: {} }, fn(module, module.exports), module.exports;
}

var kapsule_min = createCommonjsModule(function (module, exports) {
!function(n,t){module.exports=t();}(commonjsGlobal,function(){

var o,r,i;!function(u,a){r=[n,t,e(1)],void 0!==(i="function"==typeof(o=a)?o.apply(t,r):o)&&(n.exports=i);}(0,function(n,t,e){"use strict";function o(n,t){if(!(n instanceof t))throw new TypeError("Cannot call a class as a function")}Object.defineProperty(t,"__esModule",{value:!0}),t.default=function(n){var t=n.stateInit,e=void 0===t?{}:t,o=n.props,u=void 0===o?{}:o,a=n.methods,c=void 0===a?{}:a,f=n.init,l=void 0===f?function(){}:f,s=n.update,p=void 0===s?function(){}:s,d=Object.keys(u).map(function(n){return new i(n,u[n])});return function(){function n(e){return i(e,t),u(),n}var t=arguments.length>0&&void 0!==arguments[0]?arguments[0]:{},o=Object.assign({},e,{initialised:!1}),i=function(t,e){l.call(n,t,o,e),o.initialised=!0;},u=(0,r.default)(function(){o.initialised&&p.call(n,o);},1);return d.forEach(function(t){n[t.name]=function(t){var e=arguments.length>1&&void 0!==arguments[1]&&arguments[1],r=arguments.length>2&&void 0!==arguments[2]?arguments[2]:function(n,t){};return function(i){return arguments.length?(o[t]=i,r.call(n,i,o),e&&u(),n):o[t]}}(t.name,t.triggerUpdate,t.onChange);}),Object.keys(c).forEach(function(t){

),n},n.resetProps(),o._rerender=u,n}};var r=function(n){return n&&n.__esModule?n:{default:n}}(e),i=function n(t,e){var r=e.default,i=void 0===r?null:r,u=e.triggerUpdate,a=void 0===u||u,c=e.onChange,f=void 0===c?function(n,t){}:c;o(this,n),this.name=t,this.defaultVal=i,this.triggerUpdate=a,this.onChange=f;};n.exports=t.default;});},function(n,t){n.exports=function(n,t,e){

});

var Kapsule = unwrapExports(kapsule_min);

var asyncGenerator = function () {
  function AwaitValue(value) {
    this.value = value;
  }

  function AsyncGenerator(gen) {
    var front, back;

    function send(key, arg) {
      return new Promise(function (resolve, reject) {
        var request = {
          key: key,
          arg: arg,
          resolve: resolve,
          reject: reject,
          next: null
        };

        if (back) {
          back = back.next = request;
        } else {
          front = back = request;
          resume(key, arg);
        }
      });
    }

    function resume(key, arg) {
      try {
        var result = gen[key](arg);
        var value = result.value;

        if (value instanceof AwaitValue) {
          Promise.resolve(value.value).then(function (arg) {
            resume("next", arg);
          }, function (arg) {
            resume("throw", arg);
          });
        } else {
          settle(result.done ? "return" : "normal", result.value);
        }
      } catch (err) {
        settle("throw", err);
      }
    }

    function settle(type, value) {
      switch (type) {
        case "return":
          front.resolve({
            value: value,
            done: true
          });
          break;

        case "throw":
          front.reject(value);
          break;

        default:
          front.resolve({
            value: value,
            done: false
          });
          break;
      }

      front = front.next;

      if (front) {
        resume(front.key, front.arg);
      } else {
        back = null;
      }
    }

    this._invoke = send;

    if (typeof gen.return !== "function") {
      this.return = undefined;
    }
  }

  if (typeof Symbol === "function" && Symbol.asyncIterator) {
    AsyncGenerator.prototype[Symbol.asyncIterator] = function () {
      return this;
    };
  }

  AsyncGenerator.prototype.next = function (arg) {
    return this._invoke("next", arg);
  };

  AsyncGenerator.prototype.throw = function (arg) {
    return this._invoke("throw", arg);
  };

  AsyncGenerator.prototype.return = function (arg) {
    return this._invoke("return", arg);
  };

  return {
    wrap: function (fn) {
      return function () {
        return new AsyncGenerator(fn.apply(this, arguments));
      };
    },
    await: function (value) {
      return new AwaitValue(value);
    }
  };
}();

















































var toConsumableArray = function (arr) {
  if (Array.isArray(arr)) {
    for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) arr2[i] = arr[i];

    return arr2;
  } else {
    return Array.from(arr);
  }
};

var fitToBox = Kapsule({
    props: {
        bbox: { default: { width: null, height: null } },
        passes: { default: 3 }
    },
    init: function init(el, state) {
        state.el = el;
    },
    update: function update(state) {
        [].concat(toConsumableArray(Array(state.passes).keys())).some(function () {
            var startSize = parseInt(state.el.style['font-size'].split('px')[0]) || 20;
            var bbox = state.el.getBBox();
            var newSize = Math.floor(startSize * Math.min(state.bbox.width / bbox.width, state.bbox.height / bbox.height));

            if (newSize === startSize) return true; // Shortcut out

            state.el.style['font-size'] = newSize + 'px';
        });
    }
});

var ellipsis = Kapsule({
    props: {
        maxWidth: { default: Infinity }
    },
    init: function init(el, state) {
        state.el = el;
    },
    update: function update(state) {
        var origTxt = state.el.textContent;
        var nChars = Math.round(origTxt.length * state.maxWidth / state.el.getBBox().width * 1.2); // Start above
        while (--nChars && state.maxWidth / state.el.getBBox().width < 1) {
            state.el.textContent = abbreviateText(origTxt, nChars);
        }

        //

        function abbreviateText(txt, maxChars) {
            return txt.length <= maxChars ? txt : txt.substring(0, maxChars * 2 / 3) + '...' + txt.substring(txt.length - maxChars / 3, txt.length);
        }
    }
});

exports.fitToBox = fitToBox;
exports.ellipsis = ellipsis;

Object.defineProperty(exports, '__esModule', { value: true });

})));
//# sourceMappingURL=svg-text-fit.js.map
